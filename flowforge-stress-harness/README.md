# FlowForge Stress Harness (INTERNAL)

This module is for internal stress/performance testing of FlowForge.

- It is NOT part of the public API.
- It must NOT be published.
- It must NOT be used as a dependency by other FlowForge modules.

## Run

From repo root:

```bash
./gradlew :flowforge-stress-harness:bootRun
```
